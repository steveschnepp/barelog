# Dockerfile.atmega328p — minimal, ATmega328P only
# Base: Debian 13
# Size: ~500 MB

FROM barelog:base

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc-avr \
    avr-libc \
    avrdude \
    && rm -rf /var/lib/apt/lists/*

